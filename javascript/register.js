

function register() {
    var email = document.getElementById("email").value;
    var password = document.getElementById("password").value;
    var district = "@garnetvalley.org";
    var inDistrict = email.slice(district.length);

    if (!inDistrict == district) {
        retur
    }
    
    

}